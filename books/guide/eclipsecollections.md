# Eclipse Collections Support

JettraDB includes **Eclipse Collections** for advanced collection handling.

## Integration
Both the Server and Driver include `eclipse-collections` and `eclipse-collections-api`.
This allows using rich functional APIs for processing data returned by JettraDB or within custom server extensions.
